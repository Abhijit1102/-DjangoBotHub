{
  "project": "DjangoBotHub",
  "status": "running",
  "version": "1.0.0",
  "description": "A backend project with Django REST Framework, JWT authentication, Celery for background tasks, and Telegram bot integration.",
  "features": [
    "User Signup via POST /api/v1/sign-up/",
    "JWT Authentication via /api/v1/token/",
    "Profile access (protected) via GET /api/v1/profile/",
    "Health check via GET /api/v1/health-check/",
    "Telegram bot integration for /start command",
    "Background tasks handled by Celery + Redis",
    "**Telegram ChatBot endpoint via POST /chat/**"
  ],
  "api_endpoints": {
    "signup": "/api/v1/sign-up/",
    "login": "/api/v1/token/",
    "refresh_token": "/api/v1/token/refresh/",
    "profile": "/api/v1/profile/",
    "health_check": "/api/v1/health-check/",
    "chat_bot": "/chat/"
  },
  "developer_note": "Use Postman or any API testing tool to test signup/login endpoints. Make sure to include the Authorization header (Bearer token) for protected routes like /profile/."
}
